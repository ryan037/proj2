/*
 * Example with Functions
 */

class example {
  // constants
  val a = 5

  // variables
  var c : int
  var sum: int
  var index: int
  
  // function declaration
  fun add (a: int, b: int) : int {
    return a+b
  }
  
  // main statements
  fun main() {
  
    sum = 0
    index = 0
    c = add(a, 10)
    var arr: int [10]
    if (c > 10)
      print -c
    else
      print c
    println ("Hello World")
    
    // while test
    while (index <= n) {
      sum = sum + index
      index = index + 1
    }
    
    //for test
    for(i in 1..10){
      var c: int
    }
    
    //if-else test
    if(2 > 1){
      var d: int
    }else{
      var e: int
    }
      
  }
}
